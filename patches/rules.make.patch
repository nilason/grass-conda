--- include/Make/Rules.make.orig	2020-08-11 11:06:04.000000000 +0200
+++ include/Make/Rules.make	2020-08-11 11:28:35.000000000 +0200
@@ -46,8 +46,8 @@
 	GISRC=$(RUN_GISRC) \
 	GISBASE=$(RUN_GISBASE) \
 	PATH="$(ARCH_DISTDIR)/bin:$(GISBASE)/bin:$(GISBASE)/scripts:$$PATH" \
+	LD_RUN_PATH="$(BIN):$(GISBASE)/bin:$(GISBASE)/scripts:$(ARCH_LIBDIR):$(BASE_LIBDIR):$(PREFIX)/lib" \
 	PYTHONPATH="$(GRASS_PYTHONPATH)" \
-	$(LD_LIBRARY_PATH_VAR)="$(BIN):$(GISBASE)/bin:$(GISBASE)/scripts:$(ARCH_LIBDIR):$(BASE_LIBDIR):$($(LD_LIBRARY_PATH_VAR))" \
 	LC_ALL=C LANG=C LANGUAGE=C \
 	$(1)
 
